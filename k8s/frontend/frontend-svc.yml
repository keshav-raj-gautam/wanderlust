apiVersion: v1
kind: Service
metadata:
  name: wanderlust-frontend
spec:
  selector:
    app: wanderlust-frontend
  type: NodePort
  ports:
    - port: 5173
      targetPort: 5173
      nodePort: 30080 
